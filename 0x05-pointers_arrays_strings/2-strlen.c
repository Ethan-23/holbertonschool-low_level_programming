#include "holberton.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - check the code for Holberton School students.
 * @s: string length
 * Return: Always 0.
 */

int _strlen(char *s)
{
	return (strlen(s));
}

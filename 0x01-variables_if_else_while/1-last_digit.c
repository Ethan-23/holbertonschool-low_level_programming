#include <stdlib.h>
#include <time.h>
/**
 * main - This will take the last digit and tell you if its > or < 5
 *
 * Return: 0
 */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	/**place holder*/
	return (0);
}
